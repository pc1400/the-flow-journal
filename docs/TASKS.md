# Task Board

## In Progress

## Up Next
- [ ] Create workout — tap "Start Empty Workout" to create a new workout row in DB, navigate to active-workout screen, start timer
- [ ] Add exercise to workout — search/select exercise by name, add to current workout with muscle group
- [ ] Log sets — add set rows to an exercise with weight/reps, tap to mark complete
- [ ] Workout timer — live duration counter on active-workout screen using `useEffect` interval
- [ ] Finish workout — save final duration + total volume, navigate back to history
- [ ] History list — query completed workouts from DB, render as cards with date/name/volume/duration
- [ ] Exercise library — seed a default list of common exercises with muscle groups
- [ ] Delete workout — swipe-to-delete or long-press on history cards
- [ ] Edit sets — tap a completed set to modify weight/reps
- [ ] Workout templates — save a workout as a template, start new workout from template

## Done
